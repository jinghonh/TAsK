# Doxyfile 1.8.9.1

# 此文件描述了文档系统doxygen (www.doxygen.org)为项目生成文档时所使用的设置。
#
# 在双井号(##)之后的所有文本被视为注释，并放置在其前面的标签之前。
#
# 在单井号(#)之后的所有文本被视为注释，将被忽略。
# 格式为：
# 标签 = 值 [值, ...]
# 对于列表，项目也可以使用以下方式添加：
# 标签 += 值 [值, ...]
# 包含空格的值应放在引号之间(\" \")。

#---------------------------------------------------------------------------
# 项目相关配置选项
#---------------------------------------------------------------------------

# 此标签指定用于配置文件中所有后续字符的编码。默认值为UTF-8，
# 这也是在该标签首次出现之前所有文本使用的编码。Doxygen使用libiconv（或内置在libc中的iconv）
# 进行转码。有关可能的编码列表，请参见http://www.gnu.org/software/libiconv
# 默认值为：UTF-8。

DOXYFILE_ENCODING      = UTF-8

# PROJECT_NAME标签是一个单词（或用双引号括起来的一系列单词，
# 除非您使用Doxywizard），用于标识生成文档的项目。该名称将显示在
# 大多数生成页面的标题中以及其他一些位置。
# 默认值为："My Project"。

PROJECT_NAME           = "Traffic Assignment frameworK (TAsK)"

# PROJECT_NUMBER标签可用于输入项目或版本号。
# 这对归档生成的文档或者使用某些版本控制系统时可能很方便。

PROJECT_NUMBER         =

# 使用PROJECT_BRIEF标签可以为项目提供一行可选描述，
# 该描述将显示在每个页面的顶部，给浏览者对项目目的的快速了解。
# 请保持描述简短。

PROJECT_BRIEF          =

# 使用PROJECT_LOGO标签可以指定一个将包含在文档中的徽标或图标。
# 徽标的最大高度不应超过55像素，最大宽度不应超过200像素。
# Doxygen将复制徽标到输出目录。

PROJECT_LOGO           =

# OUTPUT_DIRECTORY标签用于指定生成的文档将被写入的（相对或绝对）路径。
# 如果输入相对路径，它将相对于启动doxygen的位置。
# 如果留空，则使用当前目录。

OUTPUT_DIRECTORY       = "../vendor1/Docs/"

# 如果CREATE_SUBDIRS标签设置为YES，doxygen将在每个输出格式的输出目录下
# 创建4096个子目录（在2个级别中），并将生成的文件分布在这些目录中。
# 当向doxygen提供大量源文件时，启用此选项可能很有用，
# 因为将所有生成的文件放在同一目录中可能会导致文件系统性能问题。
# 默认值为：NO。

CREATE_SUBDIRS         = No

# 如果ALLOW_UNICODE_NAMES标签设置为YES，doxygen将允许非ASCII
# 字符出现在生成文件的名称中。如果设置为NO，非ASCII字符将被转义，
# 例如_xE3_x81_x84将用于Unicode U+3044。
# 默认值为：NO。

ALLOW_UNICODE_NAMES    = NO

# OUTPUT_LANGUAGE标签用于指定doxygen生成的所有文档使用的语言。
# Doxygen将使用此信息以适当的语言生成所有常量输出。
# 可能的值有：Afrikaans，Arabic，Armenian，Brazilian，Catalan，Chinese，
# Chinese-Traditional，Croatian，Czech，Danish，Dutch，English（United States），
# Esperanto，Farsi（Persian），Finnish，French，German，Greek，Hungarian，
# Indonesian，Italian，Japanese，Japanese-en（Japanese with English messages），
# Korean，Korean-en（Korean with English messages），Latvian，Lithuanian，
# Macedonian，Norwegian，Persian（Farsi），Polish，Portuguese，Romanian，Russian，
# Serbian，Serbian-Cyrillic，Slovak，Slovene，Spanish，Swedish，Turkish，
# Ukrainian和Vietnamese。
# 默认值为：English。

OUTPUT_LANGUAGE        = English

# 如果BRIEF_MEMBER_DESC标签设置为YES，doxygen将在文件和类文档中列出的成员之后
# 包含简短的成员描述（类似于Javadoc）。设置为NO禁用此功能。
# 默认值为：YES。

BRIEF_MEMBER_DESC      = YES

# 如果REPEAT_BRIEF标签设置为YES，doxygen将在详细描述之前添加
# 成员或函数的简短描述。
#
# 注意：如果HIDE_UNDOC_MEMBERS和BRIEF_MEMBER_DESC都设置为NO，
# 则简短描述将被完全抑制。
# 默认值为：YES。

REPEAT_BRIEF           = YES

# 此标签实现了一个准智能的简短描述缩写器，用于形成
# 各种列表中的文本。此列表中的每个字符串，如果在简短描述的
# 开头文本中找到，将从文本中剥离，处理完整个列表后的结果
# 将用作注释文本。否则，简短描述将原样使用。如果留空，
# 将使用以下值（$name自动替换为实体名称）：The $name class，
# The $name widget，The $name file，is，provides，specifies，
# contains，represents，a，an和the。

ABBREVIATE_BRIEF       =

# 如果ALWAYS_DETAILED_SEC和REPEAT_BRIEF标签都设置为YES，
# 那么doxygen将生成详细的部分，即使只有一个简短的描述。
# 默认值为：NO。

ALWAYS_DETAILED_SEC    = NO

# 如果INLINE_INHERITED_MEMB标签设置为YES，doxygen将在
# 类的文档中显示该类的所有继承成员，就像这些成员是普通的类成员一样。
# 基类的构造函数、析构函数和赋值运算符将不会显示。
# 默认值为：NO。

INLINE_INHERITED_MEMB  = NO

# 如果FULL_PATH_NAMES标签设置为YES，doxygen将在文件列表和
# 头文件中的文件名前添加完整路径。如果设置为NO，
# 将使用使文件名唯一的最短路径。
# 默认值为：YES。

FULL_PATH_NAMES        = YES

# STRIP_FROM_PATH标签可用于剥离用户定义的路径部分。
# 仅当指定的字符串之一与路径的左侧部分匹配时才进行剥离。
# 该标签可用于在文件列表中显示相对路径。
# 如果留空，将使用运行doxygen的目录作为要剥离的路径。
#
# 注意，你可以在这里指定绝对路径，也可以指定相对路径，
# 相对路径将相对于启动doxygen的目录。
# 此标签要求FULL_PATH_NAMES标签设置为YES。

STRIP_FROM_PATH        =

# STRIP_FROM_INC_PATH标签可用于剥离在类的文档中提到的路径的用户定义部分，
# 该部分告诉读者要包含哪个头文件以便使用某个类。如果留空，
# 则仅使用包含类定义的头文件的名称。否则，应指定通常通过
# -I标志传递给编译器的包含路径列表。

STRIP_FROM_INC_PATH    =

# 如果SHORT_NAMES标签设置为YES，doxygen将生成更短（但可读性较差）的文件名。
# 如果你的文件系统不支持长名称（如DOS、Mac或CD-ROM上），这可能很有用。
# 默认值为：NO。

SHORT_NAMES            = NO

# 如果JAVADOC_AUTOBRIEF标签设置为YES，那么doxygen将解释
# Javadoc风格注释的第一行（直到第一个点）作为简短描述。
# 如果设置为NO，Javadoc风格将与常规Qt风格注释一样行为
# （因此需要一个显式的@brief命令来获取简短描述）。
# 默认值为：NO。

JAVADOC_AUTOBRIEF      = NO

# 如果QT_AUTOBRIEF标签设置为YES，doxygen将解释Qt风格注释的
# 第一行（直到第一个点）为简短描述。如果设置为NO，
# Qt风格将与常规Qt风格注释一样行为（因此需要一个显式的\brief命令来获取简短描述）。
# 默认值为：NO。

QT_AUTOBRIEF           = NO

# MULTILINE_CPP_IS_BRIEF标签可以设置为YES，使doxygen将
# 多行C++特殊注释块（即//!或///注释块）视为简短描述。
# 这曾经是默认行为。新的默认设置是将多行C++注释块视为详细描述。
# 如果你更喜欢旧行为，请将此标签设置为YES。
#
# 注意，将此标签设置为YES也意味着rational rose注释将不再被识别。
# 默认值为：NO。

MULTILINE_CPP_IS_BRIEF = NO

# 如果INHERIT_DOCS标签设置为YES，那么一个未记录的成员将继承
# 它重新实现的任何记录成员的文档。
# 默认值为：YES。

INHERIT_DOCS           = YES

# 如果SEPARATE_MEMBER_PAGES标签设置为YES，那么doxygen将为每个成员
# 生成一个新页面。如果设置为NO，成员的文档将成为包含它的文件/类/命名空间的一部分。
# 默认值为：NO。

SEPARATE_MEMBER_PAGES  = NO

# TAB_SIZE标签可用于设置制表符中的空格数。Doxygen
# 使用此值在代码片段中用空格替换制表符。
# 最小值：1，最大值：16，默认值：4。

TAB_SIZE               = 4

# 此标签可用于指定一些充当文档中命令的别名。
# 别名的形式为：
# name=value
# 例如添加
# "sideeffect=@par Side Effects:\n"
# 将允许你在文档中使用\sideeffect（或@sideeffect）命令，
# 这将产生一个带有"Side Effects:"标题的用户定义段落。
# 你可以在别名的值部分中使用\n插入换行符。

ALIASES                =

# 此标签可用于指定一些单词关键字映射（仅限TCL）。
# 映射的形式为"name=value"。例如添加"class=itcl::class"
# 将允许你在itcl::class意义中使用class命令。

TCL_SUBST              =

# 如果你的项目只包含C源文件，请将OPTIMIZE_OUTPUT_FOR_C标签设置为YES。
# Doxygen随后将生成更适合C的输出。
# 例如，一些名称的使用方式会有所不同。将省略所有成员的列表等。
# 默认值为：NO。

OPTIMIZE_OUTPUT_FOR_C  = NO

# 如果你的项目只包含Java或Python源文件，请将OPTIMIZE_OUTPUT_JAVA标签设置为YES。
# Doxygen随后将生成更适合该语言的输出。
# 例如，命名空间将被表示为包，限定作用域将看起来不同，等等。
# 默认值为：NO。

OPTIMIZE_OUTPUT_JAVA   = NO

# 如果你的项目包含Fortran源文件，请将OPTIMIZE_FOR_FORTRAN标签设置为YES。
# Doxygen随后将生成适合Fortran的输出。
# 默认值为：NO。

OPTIMIZE_FOR_FORTRAN   = NO

# 如果你的项目包含VHDL源文件，请将OPTIMIZE_OUTPUT_VHDL标签设置为YES。
# Doxygen随后将生成适合VHDL的输出。
# 默认值为：NO。

OPTIMIZE_OUTPUT_VHDL   = NO

# Doxygen根据其解析的文件扩展名选择要使用的解析器。
# 使用此标签，你可以为给定的扩展名分配要使用的解析器。
# Doxygen有一个内置映射，但你可以使用此标签覆盖或扩展它。
# 格式为ext=language，其中ext是文件扩展名，language是doxygen支持的解析器之一：
# IDL、Java、Javascript、C#、C、C++、D、PHP、Objective-C、Python、Fortran
# （固定格式Fortran：FortranFixed，自由格式化Fortran：FortranFree，
# 未知格式化Fortran：Fortran。在后一种情况下，解析器会尝试猜测代码是固定格式还是自由格式化的代码，
# 这是Fortran类型文件的默认值）、VHDL。例如，要使doxygen将.inc文件视为Fortran文件（默认为PHP），
# 而将.f文件视为C（默认为Fortran），使用：inc=Fortran f=C。
#
# 注意：对于没有扩展名的文件，你可以使用no_extension作为占位符。
#
# 注意，对于自定义扩展名，你还需要设置FILE_PATTERNS，
# 否则doxygen不会读取这些文件。

EXTENSION_MAPPING      =

# 如果启用了MARKDOWN_SUPPORT标签，doxygen将根据Markdown格式预处理所有注释，
# 这允许更可读的文档。有关详细信息，请参见http://daringfireball.net/projects/markdown/。
# Markdown处理的输出将由doxygen进一步处理，因此你可以混合使用doxygen、HTML和XML命令
# 以及Markdown格式。仅在出现向后兼容性问题时才禁用此选项。
# 默认值为：YES。

MARKDOWN_SUPPORT       = YES

# 当启用时，doxygen尝试将与已记录的类或命名空间对应的单词
# 链接到它们相应的文档。可以在单个情况下通过在单词前面放置%符号来阻止这种链接，
# 或者通过将AUTOLINK_SUPPORT设置为NO全局阻止。
# 默认值为：YES。

AUTOLINK_SUPPORT       = YES

# 如果你使用STL类（即std::string、std::vector等），但不想
# 将STL源作为输入包含（包含标签文件），那么你应该将此标签设置为YES，
# 以便让doxygen匹配参数包含STL类的函数声明和定义（例如，func(std::string);
# 与func(std::string) {}）。这还使涉及STL类的继承和协作图更加完整和准确。
# 默认值为：NO。

BUILTIN_STL_SUPPORT    = NO

# 如果你使用Microsoft的C++/CLI语言，应该将此选项设置为YES以启用解析支持。
# 默认值为：NO。

CPP_CLI_SUPPORT        = NO

# 如果你的项目只包含sip源文件（参见：
# http://www.riverbankcomputing.co.uk/software/sip/intro），请将SIP_SUPPORT标签设置为YES。
# Doxygen将像解析普通C++那样解析它们，但会假设所有类在没有显式保护关键字时使用公共而非私有继承。
# 默认值为：NO。

SIP_SUPPORT            = NO

# 对于Microsoft的IDL，有propget和propput属性来指示属性的getter和setter方法。
# 将此选项设置为YES将使doxygen在文档中用属性替代get和set方法。
# 这仅在这些方法确实获取或设置简单类型时有效。如果不是这种情况，或者你想无论如何都显示这些方法，
# 你应该将此选项设置为NO。
# 默认值为：YES。

IDL_PROPERTY_SUPPORT   = YES

# 如果在文档中使用成员分组，并且DISTRIBUTE_GROUP_DOC标签设置为YES，
# 那么doxygen将重用组中第一个成员（如果有）的文档为该组的其他成员提供文档。
# 默认情况下，组的所有成员必须明确记录。
# 默认值为：NO。

DISTRIBUTE_GROUP_DOC   = NO

# 将SUBGROUPING标签设置为YES，允许同类型的类成员组（例如一组公共函数）
# 作为该类型的子组放置（例如在Public Functions部分下）。设置为NO以防止子分组。
# 另外，这可以通过\nosubgrouping命令针对每个类进行设置。
# 默认值为：YES。

SUBGROUPING            = YES

# 当INLINE_GROUPED_CLASSES标签设置为YES时，类、结构体和联合体将显示
# 在它们所包含的组内（例如使用\ingroup），而不是显示在单独的页面（对于HTML和Man页面）
# 或部分（对于LaTeX和RTF）。
#
# 注意，此功能不能与SEPARATE_MEMBER_PAGES组合使用。
# 默认值为：NO。

INLINE_GROUPED_CLASSES = NO

# 当INLINE_SIMPLE_STRUCTS标签设置为YES时，只有公共数据字段或简单typedef字段的
# 结构体、类和联合体将在其定义范围（即文件、命名空间或组文档）的文档中内联显示，
# 前提是此范围有文档。如果设置为NO，结构体、类和联合体将显示在单独的页面（对于HTML和Man页面）
# 或部分（对于LaTeX和RTF）。
# 默认值为：NO。

INLINE_SIMPLE_STRUCTS  = NO

# 当启用TYPEDEF_HIDES_STRUCT标签时，结构体、联合体或枚举的typedef将被记录为
# 具有typedef名称的结构体、联合体或枚举。因此，typedef struct TypeS {} TypeT将在文档中
# 显示为名为TypeT的结构体。当禁用时，typedef将显示为文件、命名空间或类的成员。
# 而结构将被命名为TypeS。这在C代码中可能很有用，因为编码规范规定所有复合类型都应该是typedef的，
# 并且只引用typedef，而不引用标签名称。
# 默认值为：NO。

TYPEDEF_HIDES_STRUCT   = NO

# 符号查找缓存的大小可以使用LOOKUP_CACHE_SIZE设置。此缓存用于根据名称和范围解析符号。
# 由于这可能是一个昂贵的过程，并且代码中经常出现相同的符号多次，doxygen保留了一个预解析符号的缓存。
# 如果缓存太小，doxygen将变慢。如果缓存太大，内存将被浪费。
# 缓存大小由此公式给出：2^(16+LOOKUP_CACHE_SIZE)。有效范围为0..9，默认值为0，
# 对应于缓存大小为2^16=65536个符号。在运行结束时，doxygen将报告缓存使用情况，
# 并从速度角度建议最佳缓存大小。
# 最小值：0，最大值：9，默认值：0。

LOOKUP_CACHE_SIZE      = 0

#---------------------------------------------------------------------------
# 构建相关配置选项
#---------------------------------------------------------------------------

# 如果EXTRACT_ALL标签设置为YES，doxygen将假定文档中的所有实体都被记录了，
# 即使没有可用的文档。私有类成员和静态文件成员将被隐藏，除非
# EXTRACT_PRIVATE和EXTRACT_STATIC标签分别设置为YES。
# 注意：当WARNINGS设置为YES时，这也将禁用有关未记录成员的警告。
# 默认值为：NO。

EXTRACT_ALL            = NO

# 如果EXTRACT_PRIVATE标签设置为YES，类的所有私有成员将包含在文档中。
# 默认值为：NO。

EXTRACT_PRIVATE        = YES

# 如果EXTRACT_PACKAGE标签设置为YES，所有具有包或内部作用域的成员都将包含在文档中。
# 默认值为：NO。

EXTRACT_PACKAGE        = NO

# 如果EXTRACT_STATIC标签设置为YES，文件的所有静态成员将包含在文档中。
# 默认值为：NO。

EXTRACT_STATIC         = NO

# 如果EXTRACT_LOCAL_CLASSES标签设置为YES，源文件中本地定义的类（和结构体）
# 将包含在文档中。如果设置为NO，则只包含在头文件中定义的类。对Java源文件没有任何影响。
# 默认值为：YES。

EXTRACT_LOCAL_CLASSES  = YES

# 此标志仅对Objective-C代码有用。如果设置为YES，在实现部分而非接口中定义的本地方法
# 将包含在文档中。如果设置为NO，则只包含接口中的方法。
# 默认值为：NO。

EXTRACT_LOCAL_METHODS  = NO

# 如果此标志设置为YES，匿名命名空间的成员将被提取并显示在文档中，
# 作为名为'anonymous_namespace{file}'的命名空间，其中file将被替换为
# 包含匿名命名空间的文件的基本名称。默认情况下，匿名命名空间是隐藏的。
# 默认值为：NO。

EXTRACT_ANON_NSPACES   = NO

# 如果HIDE_UNDOC_MEMBERS标签设置为YES，doxygen将隐藏已记录的类或文件内的
# 所有未记录的成员。如果设置为NO，这些成员将包含在各种概览中，但不会生成文档部分。
# 如果启用了EXTRACT_ALL，此选项没有效果。
# 默认值为：NO。

HIDE_UNDOC_MEMBERS     = NO

# 如果HIDE_UNDOC_CLASSES标签设置为YES，doxygen将隐藏在类层次结构中
# 通常可见的所有未记录的类。如果设置为NO，这些类将包含在各种概览中。
# 如果启用了EXTRACT_ALL，此选项没有效果。
# 默认值为：NO。

HIDE_UNDOC_CLASSES     = NO

# 如果HIDE_FRIEND_COMPOUNDS标签设置为YES，doxygen将隐藏所有友元
# （类|结构|联合体）声明。如果设置为NO，这些声明将包含在文档中。
# 默认值为：NO。

HIDE_FRIEND_COMPOUNDS  = NO

# 如果HIDE_IN_BODY_DOCS标签设置为YES，doxygen将隐藏在函数体内找到的
# 任何文档块。如果设置为NO，这些块将附加到函数的详细文档块中。
# 默认值为：NO。

HIDE_IN_BODY_DOCS      = NO

# INTERNAL_DOCS标签确定是否包含在\internal命令之后键入的文档。
# 如果标签设置为NO，则文档将被排除。设置为YES以包含内部文档。
# 默认值为：NO。

INTERNAL_DOCS          = NO

# 如果CASE_SENSE_NAMES标签设置为NO，那么doxygen将只生成小写字母的文件名。
# 如果设置为YES，也允许大写字母。如果你的类或文件的名称只在大小写上有所不同，
# 并且如果你的文件系统支持区分大小写的文件名，这可能很有用。
# 建议Windows和Mac用户将此选项设置为NO。
# 默认值为：系统相关。

CASE_SENSE_NAMES       = YES

# 如果HIDE_SCOPE_NAMES标签设置为NO，则doxygen将在文档中显示
# 带有其完整类和命名空间作用域的成员。如果设置为YES，作用域将被隐藏。
# 默认值为：NO。

HIDE_SCOPE_NAMES       = NO

# 如果HIDE_COMPOUND_REFERENCE标签设置为NO（默认），则doxygen将
# 在页面标题中附加额外的文本，例如"Class Reference"。如果设置为YES，
# 复合引用将被隐藏。
# 默认值为：NO。

HIDE_COMPOUND_REFERENCE= NO

# 如果SHOW_INCLUDE_FILES标签设置为YES，则doxygen将在文件的文档中
# 列出该文件包含的文件列表。
# 默认值为：YES。

SHOW_INCLUDE_FILES     = YES

# 如果SHOW_GROUPED_MEMB_INC标签设置为YES，则Doxygen将为每个分组成员
# 向文档添加一个include语句，告诉读者要包含哪个文件才能使用该成员。
# 默认值为：NO。

SHOW_GROUPED_MEMB_INC  = NO

# 如果FORCE_LOCAL_INCLUDES标签设置为YES，则doxygen将在文档中使用
# 双引号而不是尖括号列出包含文件。
# 默认值为：NO。

FORCE_LOCAL_INCLUDES   = NO

# 如果INLINE_INFO标签设置为YES，则在内联成员的文档中插入标签[inline]。
# 默认值为：YES。

INLINE_INFO            = YES

# 如果SORT_MEMBER_DOCS标签设置为YES，则doxygen将按成员名称的字母顺序
# 对文件和类成员的（详细）文档进行排序。如果设置为NO，成员将以声明顺序显示。
# 默认值为：YES。

SORT_MEMBER_DOCS       = YES

# 如果SORT_BRIEF_DOCS标签设置为YES，则doxygen将按成员名称的字母顺序
# 对文件、命名空间和类成员的简短描述进行排序。如果设置为NO，成员将以声明顺序显示。
# 注意，这也将影响类列表中的类顺序。
# 默认值为：NO。

SORT_BRIEF_DOCS        = NO

# 如果SORT_MEMBERS_CTORS_1ST标签设置为YES，则doxygen将对类成员的
# （简短和详细）文档进行排序，使构造函数和析构函数首先列出。如果设置为NO，
# 构造函数将按照SORT_BRIEF_DOCS和SORT_MEMBER_DOCS定义的各自顺序显示。
# 注意：如果SORT_BRIEF_DOCS设置为NO，此选项将被忽略，不会对简短成员文档进行排序。
# 注意：如果SORT_MEMBER_DOCS设置为NO，此选项将被忽略，不会对详细成员文档进行排序。
# 默认值为：NO。

SORT_MEMBERS_CTORS_1ST = NO

# 如果SORT_GROUP_NAMES标签设置为YES，则doxygen将按字母顺序对组名层次结构进行排序。
# 如果设置为NO，组名将按其定义的顺序显示。
# 默认值为：NO。

SORT_GROUP_NAMES       = NO

# 如果SORT_BY_SCOPE_NAME标签设置为YES，类列表将按完全限定名称（包括命名空间）排序。
# 如果设置为NO，类列表将仅按类名排序，不包括命名空间部分。
# 注意：如果HIDE_SCOPE_NAMES设置为YES，此选项不是很有用。
# 注意：此选项仅适用于类列表，不适用于按字母顺序排列的列表。
# 默认值为：NO。

SORT_BY_SCOPE_NAME     = NO

# 如果启用STRICT_PROTO_MATCHING选项，并且doxygen无法正确解析函数的所有参数的类型，
# 它将拒绝匹配成员函数的原型和实现，即使只有一个候选或通过进行简单的字符串匹配
# 就很明显应该选择哪个候选。通过禁用STRICT_PROTO_MATCHING，doxygen在这种情况下
# 仍将接受原型和实现之间的匹配。
# 默认值为：NO。

STRICT_PROTO_MATCHING  = NO

# GENERATE_TODOLIST标签可用于启用（YES）或禁用（NO）待办事项列表。
# 该列表是通过在文档中放置\todo命令创建的。
# 默认值为：YES。

GENERATE_TODOLIST      = YES

# GENERATE_TESTLIST标签可用于启用（YES）或禁用（NO）测试列表。
# 该列表是通过在文档中放置\test命令创建的。
# 默认值为：YES。

GENERATE_TESTLIST      = YES

# GENERATE_BUGLIST标签可用于启用（YES）或禁用（NO）错误列表。
# 该列表是通过在文档中放置\bug命令创建的。
# 默认值为：YES。

GENERATE_BUGLIST       = YES

# GENERATE_DEPRECATEDLIST标签可用于启用（YES）或禁用（NO）弃用列表。
# 该列表是通过在文档中放置\deprecated命令创建的。
# 默认值为：YES。

GENERATE_DEPRECATEDLIST= YES

# ENABLED_SECTIONS标签可用于启用条件文档部分，这些部分由
# \if <section_label> ... \endif和\cond <section_label> ... \endcond块标记。

ENABLED_SECTIONS       =

# MAX_INITIALIZER_LINES标签确定变量或宏/定义的初始值可在文档中显示的最大行数。
# 如果初始值由超过指定行数的行组成，它将被隐藏。使用值0完全隐藏初始值。
# 无论此设置如何，可以使用文档中的\showinitializer或\hideinitializer命令
# 控制单个变量和宏/定义的值的显示。
# 最小值：0，最大值：10000，默认值：30。

MAX_INITIALIZER_LINES  = 30

# 将SHOW_USED_FILES标签设置为NO以禁用在类和结构文档底部生成的文件列表。
# 如果设置为YES，列表将提及用于生成文档的文件。
# 默认值为：YES。

SHOW_USED_FILES        = YES

# 将SHOW_FILES标签设置为NO以禁用生成Files页面。这将从Quick Index和
# Folder Tree View（如果已指定）中删除Files条目。
# 默认值为：YES。

SHOW_FILES             = YES

# 将SHOW_NAMESPACES标签设置为NO以禁用生成Namespaces页面。
# 这将从Quick Index和Folder Tree View（如果已指定）中删除Namespaces条目。
# 默认值为：YES。

SHOW_NAMESPACES        = YES

# FILE_VERSION_FILTER标签可用于指定doxygen应调用的程序或脚本，
# 以获取每个文件的当前版本（通常是从版本控制系统）。Doxygen将通过执行（通过popen()）
# 命令command input-file来调用程序，其中command是FILE_VERSION_FILTER标签的值，
# input-file是doxygen提供的输入文件的名称。程序写入标准输出的任何内容都将用作文件版本。
# 有关示例，请参见文档。

FILE_VERSION_FILTER    =

# LAYOUT_FILE标签可用于指定将由doxygen解析的布局文件。布局文件以输出格式无关的方式
# 控制生成的输出文件的全局结构。要创建代表doxygen默认设置的布局文件，
# 请使用-l选项运行doxygen。你可以在选项后面选择性地指定文件名，如果省略，
# 将使用DoxygenLayout.xml作为布局文件的名称。
#
# 注意，如果你从包含名为DoxygenLayout.xml的文件的目录运行doxygen，
# 即使LAYOUT_FILE标签为空，doxygen也会自动解析它。

LAYOUT_FILE            =

# CITE_BIB_FILES标签可用于指定一个或多个包含参考定义的bib文件。
# 这必须是.bib文件的列表。如果省略.bib扩展名，将自动添加。
# 这需要安装bibtex工具。有关更多信息，另请参见http://en.wikipedia.org/wiki/BibTeX。
# 对于LaTeX，可以使用LATEX_BIB_STYLE控制参考文献的样式。
# 要使用此功能，你需要在搜索路径中有bibtex和perl。另请参见\cite，了解如何创建引用的信息。

CITE_BIB_FILES         = "allRefs.bib"

#---------------------------------------------------------------------------
# 与警告和进度消息相关的配置选项
#---------------------------------------------------------------------------

# QUIET标签可用于打开/关闭doxygen生成到标准输出的消息。
# 如果QUIET设置为YES，则意味着消息已关闭。
# 默认值为：NO。

QUIET                  = NO

# WARNINGS标签可用于打开/关闭doxygen生成到标准错误（stderr）的警告消息。
# 如果WARNINGS设置为YES，则意味着警告已打开。
#
# 提示：在编写文档时打开警告。
# 默认值为：YES。

WARNINGS               = YES

# 如果WARN_IF_UNDOCUMENTED标签设置为YES，则doxygen将为未记录的成员生成警告。
# 如果EXTRACT_ALL设置为YES，则此标志将自动禁用。
# 默认值为：YES。

WARN_IF_UNDOCUMENTED   = YES

# 如果WARN_IF_DOC_ERROR标签设置为YES，doxygen将为文档中的潜在错误生成警告，
# 例如未记录已记录函数的某些参数，或记录不存在的参数，或错误使用标记命令。
# 默认值为：YES。

WARN_IF_DOC_ERROR      = YES

# 可以启用此WARN_NO_PARAMDOC选项，为已记录但没有其参数或返回值文档的函数获取警告。
# 如果设置为NO，doxygen将只警告错误或不完整的参数文档，而不是文档的缺失。
# 默认值为：NO。

WARN_NO_PARAMDOC       = NO

# WARN_FORMAT标签确定doxygen可以生成的警告消息的格式。字符串应包含$file、$line和$text标签，
# 这些标签将被替换为产生警告的文件和行号以及警告文本。
# 可选地，格式可以包含$version，这将被文件的版本替换（如果可以通过FILE_VERSION_FILTER获取）。
# 默认值为：$file:$line: $text。

WARN_FORMAT            = "$file:$line: $text"

# WARN_LOGFILE标签可用于指定应写入警告和错误消息的文件。
# 如果留空，输出将写入标准错误（stderr）。

WARN_LOGFILE           =

#---------------------------------------------------------------------------
# 与输入文件相关的配置选项
#---------------------------------------------------------------------------

# INPUT标签用于指定包含已记录源文件的文件和/或目录。
# 你可以输入像myfile.cpp这样的文件名或像/usr/src/myproject这样的目录。
# 用空格分隔文件或目录。
# 注意：如果此标签为空，则搜索当前目录。

INPUT                  = "./"

# 此标签可用于指定doxygen解析的源文件的字符编码。
# 在内部，doxygen使用UTF-8编码。Doxygen使用libiconv（或内置在libc中的iconv）进行转码。
# 有关可能的编码列表，请参见libiconv文档（请参见：http://www.gnu.org/software/libiconv）。
# 默认值为：UTF-8。

INPUT_ENCODING         = UTF-8

# 如果INPUT标签的值包含目录，你可以使用FILE_PATTERNS标签
# 指定一个或多个通配符模式（如*.cpp和*.h）来过滤目录中的源文件。
# 如果留空，将测试以下模式：*.c、*.cc、*.cxx、*.cpp、*.c++、*.java、*.ii、
# *.ixx、*.ipp、*.i++、*.inl、*.idl、*.ddl、*.odl、*.h、*.hh、*.hxx、*.hpp、
# *.h++、*.cs、*.d、*.php、*.php4、*.php5、*.phtml、*.inc、*.m、*.markdown、
# *.md、*.mm、*.dox、*.py、*.f90、*.f、*.for、*.tcl、*.vhd、*.vhdl、*.ucf、
# *.qsf、*.as和*.js。

FILE_PATTERNS          = *.cpp \
                         *.h

# RECURSIVE标签可用于指定是否也应搜索子目录中的输入文件。
# 默认值为：NO。

RECURSIVE              = NO

# EXCLUDE标签可用于指定应从INPUT源文件中排除的文件和/或目录。
# 这样，你可以轻松地从根目录为INPUT标签指定的目录树中排除子目录。
#
# 注意，相对路径相对于doxygen运行的目录。

EXCLUDE                =

# EXCLUDE_SYMLINKS标签可用于选择是否从输入中排除符号链接（Unix文件系统功能）的文件或目录。
# 默认值为：NO。

EXCLUDE_SYMLINKS       = NO

# 如果INPUT标签的值包含目录，你可以使用EXCLUDE_PATTERNS标签
# 指定一个或多个通配符模式，以排除这些目录中的某些文件。
#
# 注意，通配符与具有绝对路径的文件匹配，因此要排除所有测试目录，
# 例如使用模式*/test/*

EXCLUDE_PATTERNS       =

# EXCLUDE_SYMBOLS标签可用于指定一个或多个应从输出中排除的符号名称
# （命名空间、类、函数等）。符号名称可以是完全限定的名称、单词，
# 或者如果使用通配符*，则为子字符串。示例：ANamespace、AClass、
# AClass::ANamespace、ANamespace::*Test
#
# 注意，通配符与具有绝对路径的文件匹配，因此要排除所有测试目录，使用模式*/test/*

EXCLUDE_SYMBOLS        =

# EXAMPLE_PATH标签可用于指定一个或多个包含示例代码片段的文件或目录，
# 这些片段包含在文档中（请参见\include命令）。

EXAMPLE_PATH           =

# 如果EXAMPLE_PATH标签的值包含目录，你可以使用EXAMPLE_PATTERNS标签
# 指定一个或多个通配符模式（如*.cpp和*.h），以筛选目录中的源文件。
# 如果留空，则包含所有文件。

EXAMPLE_PATTERNS       =

# 如果EXAMPLE_RECURSIVE标签设置为YES，则会搜索子目录中的输入文件
# 以用于\include或\dontinclude命令，而不管RECURSIVE标签的值如何。
# 默认值为：NO。

EXAMPLE_RECURSIVE      = NO

# IMAGE_PATH标签可用于指定一个或多个包含要包含在文档中的图像的文件或目录
# （请参见\image命令）。

IMAGE_PATH             =

# INPUT_FILTER标签可用于指定doxygen应为每个输入文件调用的程序进行过滤。
# Doxygen将通过执行（通过popen()）命令来调用过滤器程序：
#
# <filter> <input-file>
#
# 其中<filter>是INPUT_FILTER标签的值，<input-file>是输入文件的名称。
# Doxygen然后将使用过滤器程序写入标准输出的输出。如果指定了FILTER_PATTERNS，
# 此标签将被忽略。
#
# 注意，过滤器不能添加或删除行；它在扫描代码之前应用，但不是在生成输出代码时应用。
# 如果添加或删除行，锚点将无法正确放置。

INPUT_FILTER           =

# FILTER_PATTERNS标签可用于按文件模式逐个指定过滤器。
# Doxygen将比较文件名与每个模式，并应用匹配时的过滤器。
# 过滤器是一个列表，格式为：pattern=filter（例如*.cpp=my_cpp_filter）。
# 有关过滤器的使用方式，请参见INPUT_FILTER。如果FILTER_PATTERNS标签为空
# 或者没有一个模式匹配文件名，则应用INPUT_FILTER。

FILTER_PATTERNS        =

# 如果FILTER_SOURCE_FILES标签设置为YES，输入过滤器（如果使用INPUT_FILTER设置）
# 也将用于过滤用于产生源文件以供浏览的输入文件（即当SOURCE_BROWSER设置为YES时）。
# 默认值为：NO。

FILTER_SOURCE_FILES    = NO

# FILTER_SOURCE_PATTERNS标签可用于按文件模式指定源过滤器。
# 模式将覆盖FILTER_PATTERN（如果有）的设置，并且也可以使用*.ext=（没有指定过滤器名）
# 来为特定模式禁用源过滤。
# 此标签需要FILTER_SOURCE_FILES标签设置为YES。

FILTER_SOURCE_PATTERNS =

# 如果USE_MDFILE_AS_MAINPAGE标签引用输入的一部分的markdown文件的名称，
# 则其内容将放置在主页面（index.html）上。如果你在例如GitHub上有一个项目，
# 想要在doxygen输出中重用引言页面，这会很有用。

USE_MDFILE_AS_MAINPAGE =

#---------------------------------------------------------------------------
# 与源代码浏览相关的配置选项
#---------------------------------------------------------------------------

# 如果SOURCE_BROWSER标签设置为YES，则将生成源文件列表。已记录的实体将与这些源文件交叉引用。
#
# 注意：要移除生成输出中的所有源代码，请确保同时将VERBATIM_HEADERS设置为NO。
# 默认值为：NO。

SOURCE_BROWSER         = NO

# 将INLINE_SOURCES标签设置为YES将直接在文档中包含函数、类和枚举的主体。
# 默认值为：NO。

INLINE_SOURCES         = NO

# 将STRIP_CODE_COMMENTS标签设置为YES将指示doxygen从生成的源代码片段中隐藏任何特殊注释块。
# 正常的C、C++和Fortran注释将始终保持可见。
# 默认值为：YES。

STRIP_CODE_COMMENTS    = YES

# 如果REFERENCED_BY_RELATION标签设置为YES，则对于每个已记录的函数，
# 将列出所有引用它的已记录函数。
# 默认值为：NO。

REFERENCED_BY_RELATION = NO

# 如果REFERENCES_RELATION标签设置为YES，则对于每个已记录的函数，
# 将列出该函数调用/使用的所有已记录实体。
# 默认值为：NO。

REFERENCES_RELATION    = NO

# 如果REFERENCES_LINK_SOURCE标签设置为YES且SOURCE_BROWSER标签设置为YES，
# 则REFERENCES_RELATION和REFERENCED_BY_RELATION列表中函数的超链接将链接到源代码。
# 否则，它们将链接到文档。
# 默认值为：YES。

REFERENCES_LINK_SOURCE = YES

# 如果启用SOURCE_TOOLTIPS（默认），则在源代码中悬停在超链接上将显示一个工具提示，
# 其中包含原型、简短描述以及定义和文档链接等附加信息。由于这将使HTML文件更大，
# 加载大文件会稍微慢一些，你可以选择禁用此功能。
# 默认值为：YES。
# 此标签需要SOURCE_BROWSER标签设置为YES。

SOURCE_TOOLTIPS        = YES

# 如果USE_HTAGS标签设置为YES，则对源代码的引用将指向由htags(1)工具
# 而不是doxygen内置的源代码浏览器生成的HTML。htags工具是GNU全局源标记系统的一部分
# （参见http://www.gnu.org/software/global/global.html）。你将需要版本4.8.6或更高版本。
#
# 要使用它，请执行以下操作：
# - 安装最新版本的global
# - 在配置文件中启用SOURCE_BROWSER和USE_HTAGS
# - 确保INPUT指向源树的根目录
# - 像往常一样运行doxygen
#
# Doxygen将调用htags（而htags将调用gtags），因此这些工具必须可从命令行获得
# （即在搜索路径中）。
#
# 结果：doxygen生成的源代码浏览器将被htags输出所取代，源代码链接将指向htags输出。
# 默认值为：NO。
# 此标签需要SOURCE_BROWSER标签设置为YES。

USE_HTAGS              = NO

# 如果VERBATIM_HEADERS标签设置为YES，doxygen将为每个类生成头文件的逐字副本，
# 指定了包含。设置为NO以禁用此功能。
# 另请参见：部分\class。
# 默认值为：YES。

VERBATIM_HEADERS       = YES

#---------------------------------------------------------------------------
# 与按字母顺序排列的类索引相关的配置选项
#---------------------------------------------------------------------------

# 如果ALPHABETICAL_INDEX标签设置为YES，将生成所有化合物的字母索引。
# 如果项目包含许多类、结构体、联合体或接口，请启用此功能。
# 默认值为：YES。

ALPHABETICAL_INDEX     = YES

# COLS_IN_ALPHA_INDEX标签可用于指定字母索引列表将分割的列数。
# 最小值：1，最大值：20，默认值：5。
# 此标签需要ALPHABETICAL_INDEX标签设置为YES。

COLS_IN_ALPHA_INDEX    = 5

# 如果项目中的所有类都以一个公共前缀开头，所有类将在字母索引中放在同一标题下。
# IGNORE_PREFIX标签可用于指定在生成索引标题时应忽略的前缀（或前缀列表）。
# 此标签需要ALPHABETICAL_INDEX标签设置为YES。

IGNORE_PREFIX          =

#---------------------------------------------------------------------------
# 与HTML输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_HTML标签设置为YES，doxygen将生成HTML输出
# 默认值为：YES。

GENERATE_HTML          = YES

# HTML_OUTPUT标签用于指定HTML文档的放置位置。如果输入相对路径，
# OUTPUT_DIRECTORY的值将放在其前面。
# 默认目录是：html。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_OUTPUT            = html

# HTML_FILE_EXTENSION标签可用于指定每个生成的HTML页面的文件扩展名
# （例如：.htm，.php，.asp）。
# 默认值为：.html。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_FILE_EXTENSION    = .html

# HTML_HEADER标签可用于为每个生成的HTML页面指定用户定义的HTML头文件。
# 如果标签为空，doxygen将生成标准头。
#
# 要获得有效的HTML，包含doxygen需要的任何脚本和样式表的头文件取决于
# 使用的配置选项（例如GENERATE_TREEVIEW设置）。强烈建议以默认头文件开始
# 使用
# doxygen -w html new_header.html new_footer.html new_stylesheet.css
# YourConfigFile
# 然后修改new_header.html文件。另请参见"Doxygen使用"部分，
# 了解如何生成doxygen通常使用的默认头文件的信息。
# 注意：头文件可能会发生变化，因此在升级到更新版本的doxygen时，
# 你通常需要重新生成默认头文件。有关可能的标记和块名称的描述，请参见文档。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_HEADER            = header.html

# HTML_FOOTER标签可用于为每个生成的HTML页面指定用户定义的HTML页脚。
# 如果标签为空，doxygen将生成标准页脚。有关如何生成默认页脚的更多信息，
# 请参见HTML_HEADER，以及特殊命令在页脚中可以使用。另请参见
# "Doxygen使用"部分，了解如何生成doxygen通常使用的默认页脚的信息。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_FOOTER            = footer.html

# HTML_STYLESHEET标签可用于指定每个HTML页面使用的用户定义的级联样式表。
# 它可用于微调HTML输出的外观。如果留空，doxygen将生成默认样式表。
# 另请参见"Doxygen使用"部分，了解如何生成doxygen通常使用的样式表的信息。
# 注意：建议使用HTML_EXTRA_STYLESHEET而不是此标签，因为它更健壮，
# 此标签（HTML_STYLESHEET）将来会过时。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_STYLESHEET        = 

# HTML_EXTRA_STYLESHEET标签可用于指定doxygen创建的标准样式表之后
# 包含的额外用户定义的级联样式表。使用此选项可以覆盖某些样式方面。
# 这比使用HTML_STYLESHEET更好，因为它不会替换标准样式表，因此对未来的更新更具鲁棒性。
# Doxygen将复制样式表文件到输出目录。
# 注意：额外样式表文件的顺序很重要（例如，列表中最后一个样式表覆盖
# 之前的样式表中的设置）。有关示例，请参见文档。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_EXTRA_STYLESHEET  = customdoxygen.css

# HTML_EXTRA_FILES标签可用于指定一个或多个额外的图像或其他源文件，
# 这些文件应复制到HTML输出目录。注意，这些文件将复制到基本HTML输出目录。
# 在HTML_HEADER和/或HTML_FOOTER文件中使用$relpath^标记加载这些文件。
# 在HTML_STYLESHEET文件中，只使用文件名。另请注意，文件将按原样复制；
# 没有可用的命令或标记。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_EXTRA_FILES       =

# HTML_COLORSTYLE_HUE标签控制HTML输出的颜色。Doxygen将根据此颜色
# 调整样式表和背景图像中的颜色。色调指定为色轮上的角度，请参见
# http://en.wikipedia.org/wiki/Hue了解更多信息。例如，值0代表红色，60是黄色，
# 120是绿色，180是青色，240是蓝色，300是紫色，360再次是红色。
# 最小值：0，最大值：359，默认值：220。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_COLORSTYLE_HUE    = 220

# HTML_COLORSTYLE_SAT标签控制HTML输出中颜色的纯度（或饱和度）。
# 值为0将仅使用灰度。值为255将产生最鲜艳的颜色。
# 最小值：0，最大值：255，默认值：100。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_COLORSTYLE_SAT    = 100

# HTML_COLORSTYLE_GAMMA标签控制应用于HTML输出中颜色的亮度分量的伽马校正。
# 低于100的值会使输出逐渐变亮，而高于100的值会使输出变暗。
# 除以100的值是实际应用的伽马值，因此80代表0.8的伽马值，220代表2.2的伽马值，
# 100不改变伽马值。
# 最小值：40，最大值：240，默认值：80。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_COLORSTYLE_GAMMA  = 80

# 如果HTML_TIMESTAMP标签设置为YES，则每个生成的HTML页面的页脚将包含页面生成的日期和时间。
# 设置为NO可以在比较多次运行的输出时有所帮助。
# 默认值为：YES。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_TIMESTAMP         = YES

# 如果HTML_DYNAMIC_SECTIONS标签设置为YES，则生成的HTML文档将包含
# 页面加载后可以隐藏和显示的部分。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_DYNAMIC_SECTIONS  = NO

# 使用HTML_INDEX_NUM_ENTRIES可以控制最初在各种树结构索引中显示的首选条目数；
# 用户可以在之后动态展开和折叠条目。Doxygen将展开树到最多指定数量的条目可见的级别
# （除非一个完全折叠的树已经超过这个数量）。因此，将条目数设置为1将默认生成完全折叠的树。
# 0是一个特殊值，代表无限数量的条目，默认情况下将产生完全展开的树。
# 最小值：0，最大值：9999，默认值：100。
# 此标签需要GENERATE_HTML标签设置为YES。

HTML_INDEX_NUM_ENTRIES = 100

# 如果GENERATE_DOCSET标签设置为YES，将生成额外的索引文件，可以用作
# Apple的Xcode 3集成开发环境的输入（请参见：http://developer.apple.com/tools/xcode/），
# 在OSX 10.5（Leopard）中引入。要创建文档集，doxygen将在HTML输出目录中生成一个Makefile。
# 运行make将在该目录中生成文档集，运行make install将文档集安装在
# ~/Library/Developer/Shared/Documentation/DocSets中，以便Xcode在启动时找到它。
# 有关更多信息，请参见http://developer.apple.com/tools/creatingdocsetswithdoxygen.html。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

GENERATE_DOCSET        = NO

# 此标签确定文档集feed的名称。文档feed提供一个集合，
# 在其中可以对来自单个提供者（如公司或产品套件）的多个文档集进行分组。
# 默认值为：Doxygen generated docs。
# 此标签需要GENERATE_DOCSET标签设置为YES。

DOCSET_FEEDNAME        = "Doxygen generated docs"

# 此标签指定应唯一标识文档集包的字符串。这应该是反向域名样式的字符串，
# 例如com.mycompany.MyDocSet。Doxygen将附加.docset到名称。
# 默认值为：org.doxygen.Project。
# 此标签需要GENERATE_DOCSET标签设置为YES。

DOCSET_BUNDLE_ID       = org.doxygen.Project

# DOCSET_PUBLISHER_ID标签指定一个字符串，应该唯一地标识文档发布者。
# 这应该是一个反向域名样式的字符串，例如com.mycompany.MyDocSet.documentation。
# 默认值为：org.doxygen.Publisher。
# 此标签需要GENERATE_DOCSET标签设置为YES。

DOCSET_PUBLISHER_ID    = org.doxygen.Publisher

# DOCSET_PUBLISHER_NAME标签标识文档发布者。
# 默认值为：Publisher。
# 此标签需要GENERATE_DOCSET标签设置为YES。

DOCSET_PUBLISHER_NAME  = Publisher

# 如果GENERATE_HTMLHELP标签设置为YES，则doxygen生成三个额外的HTML索引文件：
# index.hhp、index.hhc和index.hhk。index.hhp是可以在Windows上由Microsoft的HTML Help Workshop
# 读取的项目文件（请参见：http://www.microsoft.com/en-us/download/details.aspx?id=21138）。
#
# HTML Help Workshop包含一个可以将doxygen生成的所有HTML输出转换为单个编译的HTML文件（.chm）的编译器。
# 编译后的HTML文件现在用作Windows 98帮助格式，并将在未来的所有Windows平台上取代旧的Windows帮助格式
# （.hlp）。压缩的HTML文件还包含索引、目录，你可以在文档中搜索单词。
# HTML workshop还包含一个压缩HTML文件的查看器。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

GENERATE_HTMLHELP      = NO

# CHM_FILE标签可用于指定生成的.chm文件的文件名。如果结果不应写入html输出目录，
# 你可以在文件前面添加路径。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

CHM_FILE               =

# HHC_LOCATION标签可用于指定HTML帮助编译器（hhc.exe）的位置（包含文件名的绝对路径）。
# 如果非空，doxygen将尝试在生成的index.hhp上运行HTML帮助编译器。必须用完整路径指定文件。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

HHC_LOCATION           =

# GENERATE_CHI标志控制是否生成单独的.chi索引文件（YES）或将其包含在主.chm文件中（NO）。
# 默认值为：NO。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

GENERATE_CHI           = NO

# CHM_INDEX_ENCODING用于编码HtmlHelp索引（hhk）、内容（hhc）和项目文件内容。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

CHM_INDEX_ENCODING     =

# BINARY_TOC标志控制是否生成二进制目录表（YES）或.chm文件中的正常目录表（NO）。
# 此外，它还启用"上一个"和"下一个"按钮。
# 默认值为：NO。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

BINARY_TOC             = NO

# TOC_EXPAND标志可以设置为YES，以便为组成员向HTML帮助文档的目录和树视图添加额外的项目。
# 默认值为：NO。
# 此标签需要GENERATE_HTMLHELP标签设置为YES。

TOC_EXPAND             = NO

# 如果GENERATE_QHP标签设置为YES，并且同时设置了QHP_NAMESPACE和QHP_VIRTUAL_FOLDER，
# 将生成一个额外的索引文件，可以用作Qt的qhelpgenerator的输入，以生成HTML文档的
# Qt压缩帮助（.qch）。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

GENERATE_QHP           = NO

# 如果指定了QHG_LOCATION，QCH_FILE标签可用于指定生成的.qch文件的文件名。
# 指定的路径相对于HTML输出文件夹。
# 此标签需要GENERATE_QHP标签设置为YES。

QCH_FILE               =

# QHP_NAMESPACE标签指定生成Qt帮助项目输出时使用的命名空间。
# 有关更多信息，请参见Qt帮助项目/命名空间
# （请参见：http://qt-project.org/doc/qt-4.8/qthelpproject.html#namespace）。
# 默认值为：org.doxygen.Project。
# 此标签需要GENERATE_QHP标签设置为YES。

QHP_NAMESPACE          = org.doxygen.Project

# QHP_VIRTUAL_FOLDER标签指定生成Qt帮助项目输出时使用的命名空间。
# 有关更多信息，请参见Qt帮助项目/虚拟文件夹
# （请参见：http://qt-project.org/doc/qt-4.8/qthelpproject.html#virtual-folders）。
# 默认值为：doc。
# 此标签需要GENERATE_QHP标签设置为YES。

QHP_VIRTUAL_FOLDER     = doc

# 如果设置了QHP_CUST_FILTER_NAME标签，它指定要添加的自定义过滤器的名称。
# 有关更多信息，请参见Qt帮助项目/自定义过滤器
# （参见：http://qt-project.org/doc/qt-4.8/qthelpproject.html#custom-filters）。
# 此标签需要GENERATE_QHP标签设置为YES。

QHP_CUST_FILTER_NAME   =

# QHP_CUST_FILTER_ATTRS标签指定要添加的自定义过滤器的属性列表。
# 有关更多信息，请参见Qt帮助项目/自定义过滤器
# （参见：http://qt-project.org/doc/qt-4.8/qthelpproject.html#custom-filters）。
# 此标签需要GENERATE_QHP标签设置为YES。

QHP_CUST_FILTER_ATTRS  =

# QHP_SECT_FILTER_ATTRS标签指定此项目的过滤器部分匹配的属性列表。
# Qt帮助项目/过滤器属性（参见：
# http://qt-project.org/doc/qt-4.8/qthelpproject.html#filter-attributes）。
# 此标签需要GENERATE_QHP标签设置为YES。

QHP_SECT_FILTER_ATTRS  =

# QHG_LOCATION标签可用于指定Qt的qhelpgenerator的位置。
# 如果非空，doxygen将尝试在生成的.qhp文件上运行qhelpgenerator。
# 此标签需要GENERATE_QHP标签设置为YES。

QHG_LOCATION           =

# 如果GENERATE_ECLIPSEHELP标签设置为YES，将生成额外的索引文件，
# 这些文件与HTML文件一起形成Eclipse帮助插件。要安装此插件并使其在Eclipse的帮助内容菜单中可用，
# 需要将包含HTML和XML文件的目录内容复制到Eclipse的plugins目录中。
# plugins目录中的目录名称应该与ECLIPSE_DOC_ID值相同。
# 复制后，需要重新启动Eclipse，帮助才会出现。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

GENERATE_ECLIPSEHELP   = NO

# Eclipse帮助插件的唯一标识符。安装插件时，包含HTML和XML文件的目录名也应该有此名称。
# 每个文档集都应该有自己的标识符。
# 默认值为：org.doxygen.Project。
# 此标签需要GENERATE_ECLIPSEHELP标签设置为YES。

ECLIPSE_DOC_ID         = org.doxygen.Project

# 如果你想完全控制生成的HTML页面的布局，可能需要禁用索引并用自己的替换。
# DISABLE_INDEX标签可用于打开/关闭每个HTML页面顶部的简洁索引（选项卡）。
# 值为NO启用索引，值为YES禁用它。由于索引中的标签包含与导航树相同的信息，
# 如果你同时将GENERATE_TREEVIEW设置为YES，则可以将此选项设置为YES。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

DISABLE_INDEX          = NO

# GENERATE_TREEVIEW标签用于指定是否应生成树状索引结构以显示层次信息。
# 如果标签值设置为YES，将生成一个侧面板，其中包含树状索引结构（就像为HTML帮助生成的那样）。
# 这需要支持JavaScript、DHTML、CSS和框架的浏览器（即任何现代浏览器）。
# Windows用户可能使用HTML帮助功能会更好。通过自定义样式表（参见HTML_EXTRA_STYLESHEET），
# 可以进一步微调索引的外观。例如，doxygen生成的默认样式表有一个示例，
# 展示如何在树的根部放置图像而不是PROJECT_NAME。由于树基本上包含与选项卡索引相同的信息，
# 当启用此选项时，你可以考虑将DISABLE_INDEX设置为YES。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

GENERATE_TREEVIEW      = NO

# ENUM_VALUES_PER_LINE标签可用于设置doxygen在生成的HTML文档的概述部分中
# 每行分组的枚举值数量。
#
# 注意，值为0将完全阻止枚举值在概述部分中出现。
# 最小值：0，最大值：20，默认值：4。
# 此标签需要GENERATE_HTML标签设置为YES。

ENUM_VALUES_PER_LINE   = 4

# 如果启用树视图（参见GENERATE_TREEVIEW），则可以使用此标签
# 设置显示树的框架的初始宽度（以像素为单位）。
# 最小值：0，最大值：1500，默认值：250。
# 此标签需要GENERATE_HTML标签设置为YES。

TREEVIEW_WIDTH         = 250

# 如果EXT_LINKS_IN_WINDOW选项设置为YES，doxygen将在单独的窗口中打开
# 通过标签文件导入的外部符号链接。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

EXT_LINKS_IN_WINDOW    = NO

# 使用此标签更改HTML文档中包含的LaTeX公式作为图像的字体大小。
# 当你更改字体大小后成功运行doxygen，需要手动删除HTML输出目录中的任何form_*.png图像，
# 以强制它们重新生成。
# 最小值：8，最大值：50，默认值：10。
# 此标签需要GENERATE_HTML标签设置为YES。

FORMULA_FONTSIZE       = 10

# 使用FORMULA_TRANPARENT标签确定为公式生成的图像是否为透明PNG。
# 透明PNG在IE 6.0中不能很好地支持，但在所有现代浏览器中都支持。
#
# 注意，当更改此选项时，你需要在更改生效前删除HTML输出目录中的所有form_*.png文件。
# 默认值为：YES。
# 此标签需要GENERATE_HTML标签设置为YES。

FORMULA_TRANSPARENT    = YES

# 启用USE_MATHJAX选项以使用MathJax（参见http://www.mathjax.org）渲染LaTeX公式，
# MathJax使用客户端JavaScript进行渲染，而不是使用预渲染的位图。如果你没有安装LaTeX，
# 或者希望公式在HTML输出中看起来更漂亮，请使用此选项。启用后，
# 你可能还需要单独安装MathJax并使用MATHJAX_RELPATH选项配置其路径。
# 默认值为：NO。
# 此标签需要GENERATE_HTML标签设置为YES。

USE_MATHJAX            = NO

# 当启用MathJax时，你可以设置用于MathJax输出的默认输出格式。
# 参见MathJax网站（参见：http://docs.mathjax.org/en/latest/output.html）了解更多详情。
# 可能的值有：HTML-CSS（更慢，但兼容性最好），NativeMML（即MathML）和SVG。
# 默认值为：HTML-CSS。
# 此标签需要USE_MATHJAX标签设置为YES。

MATHJAX_FORMAT         = HTML-CSS

# 当启用MathJax时，你需要使用MATHJAX_RELPATH选项指定相对于HTML输出目录的位置。
# 目标目录应包含MathJax.js脚本。例如，如果mathjax目录位于与HTML输出目录相同的级别，
# 则MATHJAX_RELPATH应为../mathjax。默认值指向MathJax内容分发网络，
# 因此你可以快速查看结果而无需安装MathJax。但是，强烈建议在部署前
# 从http://www.mathjax.org安装本地MathJax副本。
# 默认值为：http://cdn.mathjax.org/mathjax/latest。
# 此标签需要USE_MATHJAX标签设置为YES。

MATHJAX_RELPATH        = http://cdn.mathjax.org/mathjax/latest

# MATHJAX_EXTENSIONS标签可用于指定在MathJax渲染期间应启用的一个或多个MathJax扩展名。
# 例如，MATHJAX_EXTENSIONS = TeX/AMSmath TeX/AMSsymbols
# 此标签需要USE_MATHJAX标签设置为YES。

MATHJAX_EXTENSIONS     =

# MATHJAX_CODEFILE标签可用于指定包含JavaScript代码片段的文件，这些代码片段将在MathJax代码启动时使用。
# 有关更多详情，请参见MathJax网站（参见：http://docs.mathjax.org/en/latest/output.html）。
# 有关示例，请参见文档。
# 此标签需要USE_MATHJAX标签设置为YES。

MATHJAX_CODEFILE       =

# 当启用SEARCHENGINE标签时，doxygen将为HTML输出生成一个搜索框。底层搜索引擎使用javascript和DHTML，
# 应该可以在任何现代浏览器上工作。注意，当使用HTML帮助（GENERATE_HTMLHELP）、
# Qt帮助（GENERATE_QHP）或文档集（GENERATE_DOCSET）时，已经有了搜索功能，
# 因此通常应该禁用此功能。对于大型项目，基于javascript的搜索引擎可能会很慢，
# 那么启用SERVER_BASED_SEARCH可能提供更好的解决方案。可以使用键盘进行搜索；
# 要跳转到搜索框，请使用<访问键> + S（<访问键>取决于操作系统和浏览器，
# 但通常是<CTRL>、<ALT>/<option>或两者）。在搜索框内，使用<光标向下键>跳转到搜索结果窗口，
# 可以使用<光标键>导航结果。按<Enter>选择一个项目或<escape>取消搜索。
# 当光标在搜索框内时，可以按<Shift>+<光标向下>选择过滤器选项。在这里也可以使用<光标键>
# 选择过滤器，按<Enter>或<escape>激活或取消过滤器选项。
# 默认值为：YES。
# 此标签需要GENERATE_HTML标签设置为YES。

SEARCHENGINE           = YES

# 当启用SERVER_BASED_SEARCH标签时，搜索引擎将使用Web服务器而不是使用JavaScript的Web客户端实现。
# 根据EXTERNAL_SEARCH设置，有两种基于Web服务器的搜索风格。当禁用时，doxygen将生成用于搜索的PHP脚本
# 和脚本使用的索引文件。当启用EXTERNAL_SEARCH时，索引和搜索需要由外部工具提供。
# 有关详细信息，请参见"外部索引和搜索"部分。
# 默认值为：NO。
# 此标签需要SEARCHENGINE标签设置为YES。

SERVER_BASED_SEARCH    = NO

# 当启用EXTERNAL_SEARCH标签时，doxygen将不再生成用于搜索的PHP脚本。
# 相反，搜索结果将写入一个XML文件，需要由外部索引器处理。Doxygen将调用由SEARCHENGINE_URL选项
# 指定的外部搜索引擎来获取搜索结果。
#
# Doxygen附带一个示例索引器（doxyindexer）和搜索引擎（doxysearch.cgi），
# 它们基于开源搜索引擎库Xapian（参见：http://xapian.org/）。
#
# 有关详细信息，请参见"外部索引和搜索"部分。
# 默认值为：NO。
# 此标签需要SEARCHENGINE标签设置为YES。

EXTERNAL_SEARCH        = NO

# SEARCHENGINE_URL应该指向一个由Web服务器托管的搜索引擎，
# 当启用EXTERNAL_SEARCH时，该引擎将返回搜索结果。
#
# Doxygen附带了一个示例索引器（doxyindexer）和搜索引擎
# （doxysearch.cgi），它们基于开源搜索引擎库
# Xapian（参见：http://xapian.org/）。有关详细信息，
# 请参见"外部索引和搜索"部分。
# 此标签要求SEARCHENGINE标签设置为YES。

SEARCHENGINE_URL       =

# 当同时启用SERVER_BASED_SEARCH和EXTERNAL_SEARCH时，
# 未索引的搜索数据将被写入一个文件，供外部工具进行索引。
# 使用SEARCHDATA_FILE标签可以指定此文件的名称。
# 默认文件为：searchdata.xml。
# 此标签要求SEARCHENGINE标签设置为YES。

SEARCHDATA_FILE        = searchdata.xml

# 当同时启用SERVER_BASED_SEARCH和EXTERNAL_SEARCH时，
# EXTERNAL_SEARCH_ID标签可用作项目的标识符。这与
# EXTRA_SEARCH_MAPPINGS结合使用时很有用，可以搜索多个
# 项目并将结果重定向回正确的项目。
# 此标签要求SEARCHENGINE标签设置为YES。

EXTERNAL_SEARCH_ID     =

# EXTRA_SEARCH_MAPPINGS标签可用于启用对除此配置文件
# 定义的项目之外的doxygen项目的搜索，这些项目都已
# 添加到同一个外部搜索索引中。每个项目都需要通过
# EXTERNAL_SEARCH_ID设置一个唯一的id。搜索映射然后将
# id映射到可以找到文档的相对位置。格式为：
# EXTRA_SEARCH_MAPPINGS = tagname1=loc1 tagname2=loc2 ...
# 此标签要求SEARCHENGINE标签设置为YES。

EXTRA_SEARCH_MAPPINGS  =

#---------------------------------------------------------------------------
# 与LaTeX输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_LATEX标签设置为YES，doxygen将生成LaTeX输出。
# 默认值为：YES。

GENERATE_LATEX         = NO

# LATEX_OUTPUT标签用于指定LaTeX文档将被放置的位置。如果输入
# 相对路径，则会在其前面添加OUTPUT_DIRECTORY的值。
# 默认目录为：latex。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_OUTPUT           = latex

# LATEX_CMD_NAME标签可用于指定要调用的LaTeX命令名称。
#
# 注意：当启用USE_PDFLATEX时，此选项仅用于生成HTML输出中
# 公式的位图，而不用于写入输出目录的Makefile中。
# 默认文件为：latex。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_CMD_NAME         = latex

# MAKEINDEX_CMD_NAME标签可用于指定生成LaTeX索引的
# 命令名称。
# 默认文件为：makeindex。
# 此标签要求GENERATE_LATEX标签设置为YES。

MAKEINDEX_CMD_NAME     = makeindex

# 如果COMPACT_LATEX标签设置为YES，doxygen会生成更紧凑的
# LaTeX文档。这对于小型项目可能很有用，也有助于节省
# 树木资源。
# 默认值为：NO。
# 此标签要求GENERATE_LATEX标签设置为YES。

COMPACT_LATEX          = NO

# PAPER_TYPE标签可用于设置打印机使用的纸张类型。
# 可能的值有：a4（210 x 297毫米）、letter（8.5 x 11英寸）、
# legal（8.5 x 14英寸）和executive（7.25 x 10.5英寸）。
# 默认值为：a4。
# 此标签要求GENERATE_LATEX标签设置为YES。

PAPER_TYPE             = a4

# EXTRA_PACKAGES标签可用于指定一个或多个应该包含在
# LaTeX输出中的LaTeX包名称。例如，要获取times字体，
# 您可以指定
# EXTRA_PACKAGES=times
# 如果留空，则不会包含额外的包。
# 此标签要求GENERATE_LATEX标签设置为YES。

EXTRA_PACKAGES         =

# LATEX_HEADER标签可用于为生成的LaTeX文档指定个人
# LaTeX页眉。页眉应包含直到第一章的所有内容。如果留空，
# doxygen将生成标准页眉。有关如何让doxygen将默认页眉
# 写入单独文件的信息，请参见"Doxygen使用"部分。
#
# 注意：只有在您知道自己在做什么的情况下才使用用户定义的
# 页眉！以下命令在页眉中具有特殊含义：$title、
# $datetime、$date、$doxygenversion、$projectname、
# $projectnumber、$projectbrief、$projectlogo。Doxygen将
# 用空字符串替换$title，对于其他命令的替换值，用户可
# 参考HTML_HEADER。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_HEADER           =

# LATEX_FOOTER标签可用于为生成的LaTeX文档指定个人
# LaTeX页脚。页脚应包含最后一章之后的所有内容。如果留空，
# doxygen将生成标准页脚。有关如何生成默认页脚以及页脚中
# 可以使用哪些特殊命令的信息，请参见LATEX_HEADER。
#
# 注意：只有在您知道自己在做什么的情况下才使用用户定义的
# 页脚！
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_FOOTER           =

# LATEX_EXTRA_STYLESHEET标签可用于指定额外的用户定义
# LaTeX样式表，这些样式表将在doxygen创建的标准样式表之后
# 包含。使用此选项可以覆盖某些样式方面。Doxygen将把样式表
# 文件复制到输出目录。
# 注意：额外样式表文件的顺序很重要（例如，列表中的最后一个
# 样式表会覆盖列表中前一个样式表的设置）。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_EXTRA_STYLESHEET =

# LATEX_EXTRA_FILES标签可用于指定一个或多个额外的图像或
# 其他源文件，这些文件应该被复制到LATEX_OUTPUT输出目录。
# 注意：文件将按原样复制；没有可用的命令或标记。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_EXTRA_FILES      =

# 如果PDF_HYPERLINKS标签设置为YES，生成的LaTeX将准备
# 转换为PDF（使用ps2pdf或pdflatex）。PDF文件将包含链接
# （就像HTML输出一样），而不是页面引用。这使得输出适合
# 使用PDF查看器进行在线浏览。
# 默认值为：YES。
# 此标签要求GENERATE_LATEX标签设置为YES。

PDF_HYPERLINKS         = YES

# 如果USE_PDFLATEX标签设置为YES，doxygen将使用pdflatex
# 直接从LaTeX文件生成PDF文件。设置此选项为YES，以获得
# 更高质量的PDF文档。
# 默认值为：YES。
# 此标签要求GENERATE_LATEX标签设置为YES。

USE_PDFLATEX           = YES

# 如果LATEX_BATCHMODE标签设置为YES，doxygen将在生成的
# LaTeX文件中添加\batchmode命令。这将指示LaTeX在发生
# 错误时继续运行，而不是向用户请求帮助。此选项也用于
# 在HTML中生成公式。
# 默认值为：NO。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_BATCHMODE        = NO

# 如果LATEX_HIDE_INDICES标签设置为YES，则doxygen将不会
# 在输出中包含索引章节（如文件索引、复合索引等）。
# 默认值为：NO。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_HIDE_INDICES     = NO

# 如果LATEX_SOURCE_CODE标签设置为YES，则doxygen将在
# LaTeX输出中包含带有语法高亮的源代码。
#
# 注意：显示哪些源代码还取决于其他设置，如SOURCE_BROWSER。
# 默认值为：NO。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_SOURCE_CODE      = NO

# LATEX_BIB_STYLE标签可用于指定用于参考文献的样式，
# 例如plainnat或ieeetr。有关更多信息，请参见
# http://en.wikipedia.org/wiki/BibTeX和\cite。
# 默认值为：plain。
# 此标签要求GENERATE_LATEX标签设置为YES。

LATEX_BIB_STYLE        = plain

#---------------------------------------------------------------------------
# Configuration options related to the RTF output
#---------------------------------------------------------------------------

# 如果GENERATE_RTF标签设置为YES，doxygen将生成RTF输出。生成的
# RTF输出经过优化，适用于Word 97，但可能在其他RTF
# 阅读器/编辑器中显示效果不佳。
# 默认值为：NO。

GENERATE_RTF           = NO

# RTF_OUTPUT标签用于指定RTF文档将被放置的位置。如果输入
# 相对路径，则会在其前面添加OUTPUT_DIRECTORY的值。
# 默认目录为：rtf。
# 此标签要求GENERATE_RTF标签设置为YES。

RTF_OUTPUT             = rtf

# 如果COMPACT_RTF标签设置为YES，doxygen会生成更紧凑的RTF
# 文档。这对于小型项目可能很有用，也有助于节省
# 树木资源。
# 默认值为：NO。
# 此标签要求GENERATE_RTF标签设置为YES。

COMPACT_RTF            = NO

# 如果RTF_HYPERLINKS标签设置为YES，所生成的RTF将包含
# 超链接字段。RTF文件将包含链接（就像HTML输出一样），
# 而不是页面引用。这使输出适合使用支持这些字段的Word
# 或其他Word兼容阅读器进行在线浏览。
#
# 注意：WordPad（写字板）和其他一些软件不支持链接。
# 默认值为：NO。
# 此标签要求GENERATE_RTF标签设置为YES。

RTF_HYPERLINKS         = NO

# 从文件加载样式表定义。语法类似于doxygen的配置
# 文件，即一系列赋值语句。您只需要提供替换项，
# 缺失的定义将使用默认值。
#
# 另请参见"Doxygen使用"部分，了解如何生成doxygen
# 通常使用的默认样式表。
# 此标签要求GENERATE_RTF标签设置为YES。

RTF_STYLESHEET_FILE    =

# 设置用于生成RTF文档的可选变量。语法类似于
# doxygen的配置文件。可以使用doxygen -e rtf extensionFile
# 生成模板扩展文件。
# 此标签要求GENERATE_RTF标签设置为YES。

RTF_EXTENSIONS_FILE    =

# 如果RTF_SOURCE_CODE标签设置为YES，doxygen将在RTF输出中
# 包含带有语法高亮的源代码。
#
# 注意，显示哪些源代码还取决于其他设置，如
# SOURCE_BROWSER。
# 默认值为：NO。
# 此标签要求GENERATE_RTF标签设置为YES。

RTF_SOURCE_CODE        = NO

#---------------------------------------------------------------------------
# 与man页面输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_MAN标签设置为YES，doxygen将为类和文件生成man页面。
# 默认值为：NO。

GENERATE_MAN           = NO

# MAN_OUTPUT标签用于指定man页面的存放位置。如果输入相对路径，
# OUTPUT_DIRECTORY的值将被添加到前面。在MAN_OUTPUT指定的目录内
# 将创建一个man3目录。
# 默认目录为：man。
# 此标签要求GENERATE_MAN标签设置为YES。

MAN_OUTPUT             = man

# MAN_EXTENSION标签决定添加到生成的man页面的扩展名。如果手册部分
# 不是以数字开头，将在前面添加数字3。MAN_EXTENSION标签开头的点(.)
# 是可选的。
# 默认值为：.3。
# 此标签要求GENERATE_MAN标签设置为YES。

MAN_EXTENSION          = .3

# MAN_SUBDIR标签决定在MAN_OUTPUT内创建的目录名称，man页面将放在
# 这个目录中。默认为man后跟MAN_EXTENSION（去掉开头的点）。
# 此标签要求GENERATE_MAN标签设置为YES。

MAN_SUBDIR             =

# 如果MAN_LINKS标签设置为YES且doxygen生成man输出，它将为真实man
# 页面中记录的每个实体生成一个额外的man文件。这些额外文件只引用
# 真实man页面，但没有它们，man命令将无法找到正确的页面。
# 默认值为：NO。
# 此标签要求GENERATE_MAN标签设置为YES。

MAN_LINKS              = NO

#---------------------------------------------------------------------------
# 与XML输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_XML标签设置为YES，doxygen将生成一个XML文件，其中包含
# 代码结构（包括所有文档）。
# 默认值为：NO。

GENERATE_XML           = NO

# XML_OUTPUT标签用于指定XML页面的存放位置。如果输入相对路径，
# OUTPUT_DIRECTORY的值将被添加到前面。
# 默认目录为：xml。
# 此标签要求GENERATE_XML标签设置为YES。

XML_OUTPUT             = xml

# 如果XML_PROGRAMLISTING标签设置为YES，doxygen将把程序列表（包括
# 语法高亮和交叉引用信息）转储到XML输出中。注意，启用此选项将显著
# 增加XML输出的大小。
# 默认值为：YES。
# 此标签要求GENERATE_XML标签设置为YES。

XML_PROGRAMLISTING     = YES

#---------------------------------------------------------------------------
# 与DOCBOOK输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_DOCBOOK标签设置为YES，doxygen将生成可用于生成PDF的
# Docbook文件。
# 默认值为：NO。

GENERATE_DOCBOOK       = NO

# DOCBOOK_OUTPUT标签用于指定Docbook页面的存放位置。如果输入相对路径，
# OUTPUT_DIRECTORY的值将被添加到前面。
# 默认目录为：docbook。
# 此标签要求GENERATE_DOCBOOK标签设置为YES。

DOCBOOK_OUTPUT         = docbook

# 如果DOCBOOK_PROGRAMLISTING标签设置为YES，doxygen将在DOCBOOK输出中
# 包含程序列表（包括语法高亮和交叉引用信息）。注意，启用此选项将显著
# 增加DOCBOOK输出的大小。
# 默认值为：NO。
# 此标签要求GENERATE_DOCBOOK标签设置为YES。

DOCBOOK_PROGRAMLISTING = NO

#---------------------------------------------------------------------------
# AutoGen定义输出的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_AUTOGEN_DEF标签设置为YES，doxygen将生成一个AutoGen定义
# 文件（参见http://autogen.sf.net），其中包含代码结构（包括所有文档）。
# 注意，此功能目前仍处于实验阶段且不完整。
# 默认值为：NO。

GENERATE_AUTOGEN_DEF   = NO

#---------------------------------------------------------------------------
# 与Perl模块输出相关的配置选项
#---------------------------------------------------------------------------

# 如果GENERATE_PERLMOD标签设置为YES，doxygen将生成一个Perl模块文件，
# 其中包含代码结构（包括所有文档）。
#
# 注意，此功能目前仍处于实验阶段且不完整。
# 默认值为：NO。

GENERATE_PERLMOD       = NO

# 如果PERLMOD_LATEX标签设置为YES，doxygen将生成必要的Makefile规则、
# Perl脚本和LaTeX代码，以便能够从Perl模块输出生成PDF和DVI输出。
# 默认值为：NO。
# 此标签要求GENERATE_PERLMOD标签设置为YES。

PERLMOD_LATEX          = NO

# 如果PERLMOD_PRETTY标签设置为YES，Perl模块输出将被很好地格式化，
# 以便人类读者可以解析。如果您想了解发生了什么，这很有用。另一方面，
# 如果此标签设置为NO，Perl模块输出的大小将小得多，而Perl将以相同
# 的方式解析它。
# 默认值为：YES。
# 此标签要求GENERATE_PERLMOD标签设置为YES。

PERLMOD_PRETTY         = YES

# 生成的doxyrules.make文件中的make变量名称以PERLMOD_MAKEVAR_PREFIX
# 中包含的字符串为前缀。这很有用，这样同一个Makefile包含的不同
# doxyrules.make文件不会覆盖彼此的变量。
# 此标签要求GENERATE_PERLMOD标签设置为YES。

PERLMOD_MAKEVAR_PREFIX =

#---------------------------------------------------------------------------
# 与预处理器相关的配置选项
#---------------------------------------------------------------------------

# 如果ENABLE_PREPROCESSING标签设置为YES，doxygen将评估在源文件和
# 包含文件中找到的所有C预处理器指令。
# 默认值为：YES。

ENABLE_PREPROCESSING   = YES

# 如果MACRO_EXPANSION标签设置为YES，doxygen将展开源代码中的所有
# 宏名称。如果设置为NO，则只执行条件编译。通过将EXPAND_ONLY_PREDEF
# 设置为YES，可以以受控方式执行宏展开。
# 默认值为：NO。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

MACRO_EXPANSION        = NO

# 如果EXPAND_ONLY_PREDEF和MACRO_EXPANSION标签都设置为YES，则宏展开
# 仅限于使用PREDEFINED和EXPAND_AS_DEFINED标签指定的宏。
# 默认值为：NO。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

EXPAND_ONLY_PREDEF     = NO

# 如果SEARCH_INCLUDES标签设置为YES，当找到#include时，将在INCLUDE_PATH
# 中搜索包含文件。
# 默认值为：YES。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

SEARCH_INCLUDES        = YES

# INCLUDE_PATH标签可用于指定一个或多个目录，这些目录包含不是输入文件
# 但应由预处理器处理的包含文件。
# 此标签要求SEARCH_INCLUDES标签设置为YES。

INCLUDE_PATH           =

# 您可以使用INCLUDE_FILE_PATTERNS标签指定一个或多个通配符模式（如*.h
# 和*.hpp）来过滤目录中的头文件。如果留空，将使用FILE_PATTERNS指定
# 的模式。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

INCLUDE_FILE_PATTERNS  =

# PREDEFINED标签可用于指定一个或多个宏名称，这些宏在预处理器启动前
# 就已定义（类似于gcc的-D选项）。标签的参数是形式为name或name=definition
# （无空格）的宏列表。如果省略定义和"="，则假定为"=1"。要防止通过#undef
# 取消定义宏定义或递归展开，请使用:=运算符而不是=运算符。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

PREDEFINED             =

# 如果MACRO_EXPANSION和EXPAND_ONLY_PREDEF标签都设置为YES，则此标签
# 可用于指定应展开的宏名称列表。将使用在源代码中找到的宏定义。如果您
# 想使用覆盖源代码中找到的定义的不同宏定义，请使用PREDEFINED标签。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

EXPAND_AS_DEFINED      =

# 如果SKIP_FUNCTION_MACROS标签设置为YES，doxygen的预处理器将删除所有
# 对单独在一行上、具有全大写名称且不以分号结尾的函数类宏的引用。此类
# 函数宏通常用于样板代码，如果不删除，会使解析器混淆。
# 默认值为：YES。
# 此标签要求ENABLE_PREPROCESSING标签设置为YES。

SKIP_FUNCTION_MACROS   = YES

#---------------------------------------------------------------------------
# 与外部引用相关的配置选项
#---------------------------------------------------------------------------

# TAGFILES标签可用于指定一个或多个标签文件。对于每个标签文件，应添加
# 外部文档的位置。不带此位置的标签文件格式如下：
# TAGFILES = file1 file2 ...
# 添加标签文件位置的方法如下：
    # TAGFILES = file1=loc1 "file2 = loc2" ...
    # where loc1 and loc2 can be relative or absolute paths or URLs. See the
    # section "Linking to external documentation" for more information about the use
    # of tag files.
    # Note: Each tag file must have a unique name (where the name does NOT include
    # the path). If a tag file is not located in the directory in which doxygen is
    # run, you must also specify the path to the tagfile here.

    TAGFILES               =

# 当在GENERATE_TAGFILE后指定文件名时，doxygen将基于其读取的输入文件创建一个标签文件。
# 有关标签文件使用的更多信息，请参见"链接到外部文档"部分。

GENERATE_TAGFILE       =

# 如果ALLEXTERNALS标签设置为YES，所有外部类都将列在类索引中。如果设置为NO，
# 则只列出继承的外部类。
# 默认值为：NO。

ALLEXTERNALS           = NO

# 如果EXTERNAL_GROUPS标签设置为YES，所有外部组都将列在模块索引中。如果设置为NO，
# 则只列出当前项目的组。
# 默认值为：YES。

EXTERNAL_GROUPS        = YES

# 如果EXTERNAL_PAGES标签设置为YES，所有外部页面都将列在相关页面索引中。如果设置为NO，
# 则只列出当前项目的页面。
# 默认值为：YES。

EXTERNAL_PAGES         = YES

# PERL_PATH应该是perl脚本解释器的绝对路径和名称（即'which perl'的结果）。
# 默认文件（带绝对路径）是：/usr/bin/perl。

PERL_PATH              = /usr/bin/perl

#---------------------------------------------------------------------------
# 与dot工具相关的配置选项
#---------------------------------------------------------------------------

# 如果CLASS_DIAGRAMS标签设置为YES，doxygen将为具有基类或超类的类生成类图（HTML和LaTeX格式）。
# 将此标签设置为NO将关闭图表。请注意，即使HAVE_DOT被禁用，此选项也能工作，
# 但建议安装和使用dot，因为它能生成更强大的图形。
# 默认值为：YES。

CLASS_DIAGRAMS         = YES

# 您可以在doxygen注释中使用\msc命令定义消息序列图。doxygen将运行mscgen工具
# （参见：http://www.mcternan.me.uk/mscgen/）来生成图表并将其插入文档中。
# MSCGEN_PATH标签允许您指定mscgen工具所在的目录。如果留空，则假定在默认搜索路径中找到该工具。

MSCGEN_PATH            =

# 您可以在doxygen文档中包含使用dia制作的图表。doxygen将运行dia来生成图表并将其插入文档中。
# DIA_PATH标签允许您指定dia二进制文件所在的目录。如果留空，则假定在默认搜索路径中找到dia。

DIA_PATH               =

# 如果设置为YES，继承和协作图将隐藏未记录或不是类的目标的继承和使用关系。
# 默认值为：YES。

HIDE_UNDOC_RELATIONS   = YES

# 如果您将HAVE_DOT标签设置为YES，则doxygen将假定dot工具可从路径中获得。
# 此工具是Graphviz的一部分（参见：http://www.graphviz.org/），这是AT&T和Lucent Bell Labs的图形可视化工具包。
# 如果此选项设置为NO，则本节中的其他选项无效。
# 默认值为：NO。

HAVE_DOT               = YES

# DOT_NUM_THREADS指定doxygen允许并行运行的dot调用数量。当设置为0时，doxygen将基于系统中可用的处理器数量来确定。
# 您可以将其显式设置为大于0的值，以控制CPU负载和处理速度之间的平衡。
# 最小值：0，最大值：32，默认值：0。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_NUM_THREADS        = 0

# 当您想要在doxygen生成的dot文件中使用不同外观的字体时，可以使用DOT_FONTNAME指定字体名称。
# 您需要确保dot能够找到该字体，这可以通过将其放在标准位置、设置DOTFONTPATH环境变量
# 或设置DOT_FONTPATH到包含字体的目录来实现。
# 默认值为：Helvetica。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_FONTNAME           = Helvetica

# DOT_FONTSIZE标签可用于设置dot图形中字体的大小（以点为单位）。
# 最小值：4，最大值：24，默认值：10。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_FONTSIZE           = 10

# 默认情况下，doxygen将告诉dot使用DOT_FONTNAME指定的默认字体。
# 如果您使用DOT_FONTNAME指定了不同的字体，可以使用此标签设置dot可以找到它的路径。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_FONTPATH           =

# 如果CLASS_GRAPH标签设置为YES，则doxygen将为每个记录的类生成一个显示直接和间接继承关系的图。
# 将此标签设置为YES将强制CLASS_DIAGRAMS标签为NO。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

CLASS_GRAPH            = YES

# 如果COLLABORATION_GRAPH标签设置为YES，则doxygen将为每个记录的类生成一个显示该类与其他记录类
# 的直接和间接实现依赖关系（继承、包含和类引用变量）的图。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

COLLABORATION_GRAPH    = YES

# 如果GROUP_GRAPHS标签设置为YES，则doxygen将为组生成一个显示直接组依赖关系的图。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

GROUP_GRAPHS           = YES

# 如果UML_LOOK标签设置为YES，doxygen将以类似于OMG统一建模语言的风格生成继承和协作图。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

UML_LOOK               = YES

# 如果启用了UML_LOOK标签，字段和方法将显示在类节点内部。如果有许多字段或方法和许多节点，
# 图形可能会变得太大而无法使用。UML_LIMIT_NUM_FIELDS阈值限制每种类型的项目数量，
# 以使大小更易于管理。将其设置为0表示无限制。请注意，在强制执行限制之前，
# 阈值可能会超过50%。因此，当您将阈值设置为10时，最多可能出现15个字段，
# 但如果数量超过15，则显示的字段总数将限制为10。
# 最小值：0，最大值：100，默认值：10。
# 此标签要求HAVE_DOT标签设置为YES。

UML_LIMIT_NUM_FIELDS   = 10

# 如果TEMPLATE_RELATIONS标签设置为YES，则继承和协作图将显示模板与其实例之间的关系。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

TEMPLATE_RELATIONS     = NO

# 如果INCLUDE_GRAPH、ENABLE_PREPROCESSING和SEARCH_INCLUDES标签都设置为YES，
# 则doxygen将为每个记录的文件生成一个显示该文件与其他记录文件的直接和间接包含依赖关系的图。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

INCLUDE_GRAPH          = YES

# 如果INCLUDED_BY_GRAPH、ENABLE_PREPROCESSING和SEARCH_INCLUDES标签都设置为YES，
# 则doxygen将为每个记录的文件生成一个显示该文件与其他记录文件的直接和间接包含依赖关系的图。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

INCLUDED_BY_GRAPH      = YES

# 如果CALL_GRAPH标签设置为YES，则doxygen将为每个全局函数或类方法生成一个调用依赖图。
#
# 请注意，启用此选项将显著增加运行时间。因此在大多数情况下，最好只使用\callgraph命令
# 为选定的函数启用调用图。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

CALL_GRAPH             = NO

# 如果CALLER_GRAPH标签设置为YES，则doxygen将为每个全局函数或类方法生成一个调用者依赖图。
#
# 请注意，启用此选项将显著增加运行时间。因此在大多数情况下，最好只使用\callergraph命令
# 为选定的函数启用调用者图。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

CALLER_GRAPH           = NO

# 如果GRAPHICAL_HIERARCHY标签设置为YES，则doxygen将生成一个图形化的类层次结构，
# 而不是文本层次结构。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

GRAPHICAL_HIERARCHY    = YES

# 如果DIRECTORY_GRAPH标签设置为YES，则doxygen将以图形方式显示目录之间的依赖关系。
# 依赖关系由目录中文件之间的#include关系确定。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

DIRECTORY_GRAPH        = YES

# DOT_IMAGE_FORMAT标签可用于设置由dot生成的图像的格式。有关图像格式的说明，
# 请参见dot工具文档中的输出格式部分（Graphviz，参见：http://www.graphviz.org/）。
# 注意：如果选择svg，需要将HTML_FILE_EXTENSION设置为xhtml，以使SVG文件在IE 9+中可见
# （其他浏览器没有此要求）。
# 可能的值有：png、jpg、gif、svg、png:gd、png:gd:gd、png:cairo、png:cairo:gd、
# png:cairo:cairo、png:cairo:gdiplus、png:gdiplus和png:gdiplus:gdiplus。
# 默认值为：png。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_IMAGE_FORMAT       = png

# 如果DOT_IMAGE_FORMAT设置为svg，则可以将此选项设置为YES以启用交互式SVG图像的生成，
# 这些图像允许缩放和平移。
#
# 注意：这需要除Internet Explorer之外的现代浏览器。经过测试并确认可用的有Firefox、
# Chrome、Safari和Opera。
# 注意：对于IE 9+，需要将HTML_FILE_EXTENSION设置为xhtml以使SVG文件可见。
# 较旧版本的IE不支持SVG。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

INTERACTIVE_SVG        = NO

# DOT_PATH标签可用于指定dot工具的路径。如果留空，则假定dot工具可以在路径中找到。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_PATH               =

# DOTFILE_DIRS标签可用于指定一个或多个包含dot文件的目录，这些文件将包含在文档中
# （参见\dotfile命令）。
# 此标签要求HAVE_DOT标签设置为YES。

DOTFILE_DIRS           =

# MSCFILE_DIRS标签可用于指定一个或多个包含msc文件的目录，这些文件将包含在文档中
# （参见\mscfile命令）。

MSCFILE_DIRS           =

# DIAFILE_DIRS标签可用于指定一个或多个包含dia文件的目录，这些文件将包含在文档中
# （参见\diafile命令）。

DIAFILE_DIRS           =

# 使用plantuml时，PLANTUML_JAR_PATH标签应指定java可以找到plantuml.jar文件的路径。
# 如果留空，则假定PlantUML未使用或在预处理步骤中未调用。在这种情况下，当doxygen
# 遇到\startuml命令时将生成警告，并且不会为图表生成输出。

PLANTUML_JAR_PATH      =

# 使用plantuml时，将在指定路径中搜索由plantuml块中的!include语句指定的文件。

PLANTUML_INCLUDE_PATH  =

# DOT_GRAPH_MAX_NODES标签可用于设置图中将显示的最大节点数。如果图中的节点数超过此值，
# doxygen将截断图形，这通过将节点表示为红色框来可视化。注意，如果图中根节点的直接
# 子节点数已经大于DOT_GRAPH_MAX_NODES，则根本不会显示图形。还要注意，图形的大小可以
# 通过MAX_DOT_GRAPH_DEPTH进一步限制。
# 最小值：0，最大值：10000，默认值：50。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_GRAPH_MAX_NODES    = 50

# MAX_DOT_GRAPH_DEPTH标签可用于设置由dot生成的图形的最大深度。深度值为3意味着只显示
# 通过最多3条边从根节点可达的节点。距离根节点更远的节点将被省略。注意，将此选项设置
# 为1或2可能会大大减少大型代码库所需的计算时间。还要注意，图形的大小可以通过
# DOT_GRAPH_MAX_NODES进一步限制。使用深度0表示没有深度限制。
# 最小值：0，最大值：1000，默认值：0。
# 此标签要求HAVE_DOT标签设置为YES。

MAX_DOT_GRAPH_DEPTH    = 0

# 将DOT_TRANSPARENT标签设置为YES以生成具有透明背景的图像。默认情况下禁用此功能，
# 因为Windows上的dot似乎默认不支持此功能。
#
# 警告：根据使用的平台，启用此选项可能会导致图形边缘的标签抗锯齿效果不佳
# （即它们变得难以阅读）。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_TRANSPARENT        = NO

# 将DOT_MULTI_TARGETS标签设置为YES以允许dot在一次运行中生成多个输出文件
# （即在命令行上使用多个-o和-T选项）。这使dot运行更快，但由于只有较新版本的dot
# （>1.8.10）支持此功能，因此默认情况下禁用此功能。
# 默认值为：NO。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_MULTI_TARGETS      = YES

# 如果GENERATE_LEGEND标签设置为YES，doxygen将生成一个图例页面，解释dot生成的
# 图形中各种框和箭头的含义。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

GENERATE_LEGEND        = YES

# 如果DOT_CLEANUP标签设置为YES，doxygen将删除用于生成各种图形的中间dot文件。
# 默认值为：YES。
# 此标签要求HAVE_DOT标签设置为YES。

DOT_CLEANUP            = YES